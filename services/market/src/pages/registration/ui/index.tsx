import React, { FC } from "react"
import './index.sass'

export const Registration: FC = () => {
    return (
        <div className="registration">
            Registration
        </div>
    )
}